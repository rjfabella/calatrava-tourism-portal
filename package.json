{
  "name": "calatrava-tourism-portal",
  "private": true,
  "description": "Tourism portal for Calatrava, Romblon — static site hosted on GitHub Pages with a browser-based admin editor that publishes via the GitHub API.",
  "scripts": {
    "dev": "npx --yes http-server . -p 5173 -c-1"
  }
}
